import styles from '../styles/components/Feed.styles.module.css'

const Feed = () => {
    return (
        <article className={ styles.Feed }>
            Feed
        </article>
    )
}

export default Feed
